<?xml version="1.0" encoding="utf-8"?>
<!--
  Accessibility service configuration. Drives the foreground-app watcher
  that blocks unauthorized entry into bound apps.

  canRetrieveWindowContent is TRUE: the gesture-nav quick-switch into an
  already-resident app fires TYPE_WINDOWS_CHANGED with a null packageName,
  so the service falls back to rootInActiveWindow.packageName to identify
  the foreground app. Only the package name is read — no window text is
  scraped. (The app is distributed as a sideloaded APK, not via Play, so
  the earlier Play Console declaration concern no longer applies.)
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:summary="@string/accessibility_service_label" />
